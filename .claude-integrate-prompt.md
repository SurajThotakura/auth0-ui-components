Scaffold Vue example app and integrate component.

1. Create `examples/vue/` based on `examples/react-spa-npm/` (Vue equivalents)
2. Read `packages/vue/SKILL.md` "Example App Setup" section for patterns
3. Add view for organization-details at route `/organization-details`
4. Add nav link in NavBar.vue
5. Test: `cd examples/vue && pnpm dev` → http://localhost:5173/organization-details
